proxy_http_version 1.1;

location /api/ { proxy_pass http://backend.sprojex.in:8080/; }

#location /api/ { proxy_pass http://<upstream-name>/; } # in upstream, mention the server domain with port number


location /health {
  stub_status on;
  access_log off;
}